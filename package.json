{
  "name": "@vaur94/opencode2-memory",
  "version": "0.3.0",
  "description": "Persistent linked memory MCP server for OpenCode V2",
  "type": "module",
  "main": "./dist/server.js",
  "exports": "./dist/server.js",
  "bin": {
    "opencode2-memory": "dist/server.js"
  },
  "files": [
    "dist/server.js",
    "ARCHITECTURE.md"
  ],
  "scripts": {
    "build": "bun build src/index.ts --target bun --format esm --packages external --outfile dist/server.js",
    "check": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.3.14"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.2.0"
  }
}
